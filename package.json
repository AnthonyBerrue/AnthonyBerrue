{
  "name": "anthony-profile",
  "version": "3.0.0",
  "private": true,
  "description": "Dynamic GitHub profile README generator (Bun + TS + GitHub API)",
  "engines": {
    "bun": ">=1.1.0"
  },
  "scripts": {
    "generate": "bun run src/generate.ts",
    "format": "bunx --bun @biomejs/biome format --write .",
    "lint": "bunx --bun @biomejs/biome lint ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.2",
    "@types/node": "^24.9.2"
  },
  "type": "module"
}